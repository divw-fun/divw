[package]
name = "divw_math"
version = "0.1.0"
edition = "2021"

[dependencies]
anchor-lang = "0.32.1"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ["cfg(feature, values(\"anchor-debug\"))"] }
