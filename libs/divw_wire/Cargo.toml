[package]
name = "divw_wire"
version = "0.1.0"
edition = "2021"

[dependencies]
anchor-lang = "0.29.0"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ["cfg(feature, values(\"anchor-debug\"))"] }
